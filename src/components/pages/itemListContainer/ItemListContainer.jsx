import React from "react";

const ItemListContainer = (greeting) => {
  return <h2> Hola, {greeting.visitante}, bienvenido! </h2>;
};

// Agregar una prop greeting, muestra un mensaje dentro del contenedor.

export default ItemListContainer;
